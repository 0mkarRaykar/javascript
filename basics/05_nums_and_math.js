const score = 400
console.log(score);

const balance = 35.085
console.log(balance.toFixed(1));

console.log(balance.toPrecision(4));

const hundreds = 10000000
console.log(hundreds.toLocaleString('en-IN')); // default use usa style numbering; use en-IN to convert in INR style
